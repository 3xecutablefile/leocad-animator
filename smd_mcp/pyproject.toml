[project]
name = "smd-mcp"
version = "0.1.0"
description = "MCP server for StopMotionDigital animation"
requires-python = ">=3.12"
dependencies = ["fastmcp>=0.6"]

[project.scripts]
smd-mcp = "smd_mcp.server:main"
